<div class="container">
  <div class="card mt-4">
    <div class="card-body">
      <p>
        <strong>Name:</strong>
        <%= @agency.name %>
      </p>

      <p>
        <strong>Cover image:</strong>
        <%= @agency.cover_image %>
      </p>

      <p>
        <strong>Logo:</strong>
        <%= @agency.logo %>
      </p>

      <%= link_to 'Edit', edit_agency_path(@agency), class: "btn btn-primary" %> |
      <%= link_to 'Back', agencies_path, class: "btn btn-secondary" %>      
    </div>
  </div>
</div>
