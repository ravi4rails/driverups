<%= search_form_for @q, url: :admin_agencies, method: :get do |f| %>
  <div class="form-group">
    <%= f.label :name %>
    <%= f.search_field :name_cont, class: "form-control" %>      
  </div>
  <div class="actions">
    <%= submit_tag 'Search',  class: 'btn btn-primary' %>
    <%= link_to 'Reset', admin_agencies_path, class: 'btn btn-secondary' %>
  </div>  
<% end %>